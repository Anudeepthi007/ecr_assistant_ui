# ---------------------------------------------------------------------------
# ECR Intelligence Agent - backend dependencies
# ---------------------------------------------------------------------------
# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sse-starlette>=2.1.0

# Validation / settings
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# Persistence
SQLAlchemy>=2.0.30
alembic>=1.13.0
# Data lives in CSV files (backend/data). SQLAlchemy queries a working copy
# loaded from them. The PostgreSQL driver is only needed when DATABASE_URL points
# that working copy at PostgreSQL (e.g. for VECTOR_STORE=pgvector).
psycopg[binary]>=3.2.0 ; platform_python_implementation == "CPython"

# Agentic AI
langgraph>=0.2.0
langchain-core>=0.3.0

# Analysis
networkx>=3.3
numpy>=1.26.0

# HTTP / LLM providers (providers are called over plain HTTP - no vendor SDK required)
httpx>=0.27.0

# Logging
structlog>=24.1.0

# Testing
pytest>=8.0.0
pytest-xdist>=3.6.0
